# 🕌 نظام إدارة تعليم القرآن الكريم

منصة متكاملة لإدارة مدارس وتحفيظ القرآن الكريم تشمل إدارة الطلاب، المعلمين، المقررات، الحضور، والتقارير.

## ✨ المميزات الرئيسية

### 📊 لوحة التحكم
- إحصائيات حية وشاملة
- رسوم بيانية تفاعلية
- نظرة عامة على الأداء
- الأنشطة الحديثة

### 👥 إدارة الطلاب
- تسجيل الطلاب الجدد
- إدارة البيانات الشخصية
- متابعة المستوى التعليمي
- سجل الحضور والدرجات
- معلومات أولياء الأمور

### 👨‍🏫 إدارة المعلمين
- بيانات المعلمين وتخصصاتهم
- إدارة المقررات
- متابعة الأداء
- الجداول الدراسية

### 📚 إدارة المقررات
- إنشاء المقررات الدراسية
- إدارة المناهج
- الجداول الزمنية
- التسجيل في المقررات

### 📅 نظام الحضور
- تسجيل الحضور اليومي
- متابعة الغياب
- تقارير الحضور
- إشعارات أولياء الأمور

### 🎯 نظام التقييم
- إدارة الدرجات والتقييمات
- تقارير الأداء
- شهادات الطلاب
- متابعة التقدم

### 👨‍👩‍👧‍👦 متابعة أولياء الأمور
- سجل الاتصالات
- تقارير الأداء
- اجتماعات أولياء الأمور
- نظام التنبيهات

### 📈 التقارير والإحصائيات
- تقارير شاملة
- إحصائيات تفصيلية
- رسوم بيانية متقدمة
- تصدير البيانات

## 🛠 التقنيات المستخدمة

### Frontend
- **React.js 18** - واجهة المستخدم
- **CSS3** - التصميم والتنسيق
- **Chart.js** - الرسوم البيانية
- **React Router** - التنقل بين الصفحات
- **Axios** - الاتصال بالخادم
- **Lucide React** - الأيقونات

### Backend
- **Node.js** - بيئة التشغيل
- **Express.js** - إطار العمل
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ODM لقاعدة البيانات
- **JWT** - المصادقة
- **bcryptjs** - تشفير كلمات المرور

### التطوير والإنتاج
- **Docker** - containerization
- **Docker Compose** - إدارة الحاويات
- **Nginx** - خادم الويب
- **Git** - التحكم بالإصدارات

## 🚀 التثبيت والتشغيل

### المتطلبات الأساسية
- Node.js 18+
- MongoDB 5.0+
- npm أو yarn

### طريقة التشغيل التقليدية

1. **Clone المشروع:**
```bash
git clone https://github.com/your-username/quran-learning-platform.git
cd quran-learning-platform
```

1. تشغيل الباكند:

```bash
cd backend
npm install
cp .env.example .env
# تعديل ملف .env بإعداداتك
npm run seed  # لتحميل البيانات التجريبية
npm start
```

1. تشغيل الفرونتند:

```bash
cd frontend
npm install
npm start
```

التشغيل باستخدام Docker

```bash
# تشغيل كامل للتطبيق
docker-compose up -d

# إعادة بناء الصور
docker-compose up -d --build

# إيقاف التطبيق
docker-compose down
```

البيانات التجريبية

يأتي النظام مع بيانات تجريبية جاهزة للاستخدام:

· المدير:
  · البريد: admin@quran-school.com
  · كلمة المرور: admin123
· المعلم:
  · البريد: teacher1@quran-school.com
  · كلمة المرور: teacher123

 هيكل المشروع

```
quran-learning-platform/
├── 📁 frontend/                 # تطبيق React
│   ├── 📁 public/              # الملفات العامة
│   ├── 📁 src/                 # شفرة المصدر
│   │   ├── 📁 components/      # المكونات
│   │   │   ├── 📁 Dashboard/   # لوحة التحكم
│   │   │   ├── 📁 Students/    # إدارة الطلاب
│   │   │   ├── 📁 Teachers/    # إدارة المعلمين
│   │   │   ├── 📁 Courses/     # إدارة المقررات
│   │   │   ├── 📁 Attendance/  # الحضور والغياب
│   │   │   ├── 📁 Grades/      # الدرجات والتقييم
│   │   │   ├── 📁 ParentFollowup/ # متابعة أولياء الأمور
│   │   │   ├── 📁 Reports/     # التقارير
│   │   │   └── 📁 Shared/      # المكونات المشتركة
│   │   ├── 📁 pages/           # الصفحات
│   │   ├── 📁 services/        # خدمات API
│   │   ├── 📁 styles/          # التنسيقات
│   │   ├── App.js              # التطبيق الرئيسي
│   │   └── index.js            # نقطة الدخول
│   ├── package.json
│   └── Dockerfile
├── 📁 backend/                  # خادم Node.js
│   ├── 📁 controllers/         # المتحكمات
│   │   ├── studentController.js
│   │   ├── teacherController.js
│   │   ├── courseController.js
│   │   ├── attendanceController.js
│   │   ├── gradeController.js
│   │   ├── parentFollowupController.js
│   │   ├── reportController.js
│   │   └── authController.js
│   ├── 📁 models/              # نماذج البيانات
│   │   ├── Student.js
│   │   ├── Teacher.js
│   │   ├── Course.js
│   │   ├── Attendance.js
│   │   ├── Grade.js
│   │   ├── ParentFollowup.js
│   │   ├── Enrollment.js
│   │   └── User.js
│   ├── 📁 routes/              # مسارات API
│   │   ├── students.js
│   │   ├── teachers.js
│   │   ├── courses.js
│   │   ├── attendance.js
│   │   ├── grades.js
│   │   ├── parentFollowup.js
│   │   ├── reports.js
│   │   └── auth.js
│   ├── 📁 middleware/          # middleware
│   │   └── auth.js
│   ├── 📁 config/              # الإعدادات
│   │   └── database.js
│   ├── 📁 seeds/               # البيانات التجريبية
│   │   └── seedDatabase.js
│   ├── server.js               # نقطة الدخول
│   ├── package.json
│   └── Dockerfile
├── 📁 docs/                    # الوثائق
├── docker-compose.yml          # إعدادات Docker
└── README.md                   # هذا الملف
```

 نماذج البيانات

 المستخدمون (Users)

· المديرين والمعلمين
· نظام المصادقة
· إدارة الصلاحيات

 الطلاب (Students)

· البيانات الشخصية
· المعلومات الأكاديمية
· معلومات أولياء الأمور
· السجل التعليمي

 المعلمون (Teachers)

· البيانات الشخصية
· التخصصات العلمية
· الخبرات والمؤهلات
· الجداول الدراسية

 المقررات (Courses)

· المعلومات الأساسية
· الجداول الزمنية
· المتطلبات والأهداف
· التسعير والسعة

 التسجيلات (Enrollments)

· تسجيل الطلاب في المقررات
· حالة التسجيل
· التواريخ والنتائج

 الحضور (Attendance)

· السجل اليومي
· حالات الحضور
· الإحصائيات والتقارير

الدرجات (Grades)

· نتائج التقييمات
· أنواع الامتحانات
· الإحصائيات والنتائج

📞 متابعة أولياء الأمور (ParentFollowup)

· سجل الاتصالات
· المهام والمتابعات
· التنبيهات والإشعارات

🔌 API Endpoints

 المصادقة

الطريقة المسار الوصف الصلاحيات
POST /api/auth/register إنشاء حساب جديد عام
POST /api/auth/login تسجيل الدخول عام
GET /api/auth/me بيانات المستخدم الحالي مستخدم
PUT /api/auth/updatedetails تحديث البيانات مستخدم
PUT /api/auth/updatepassword تحديث كلمة المرور مستخدم

👥 الطلاب

الطريقة المسار الوصف الصلاحيات
GET /api/students جلب جميع الطلاب مستخدم
POST /api/students إضافة طالب جديد مدير
GET /api/students/:id جلب بيانات طالب مستخدم
PUT /api/students/:id تحديث بيانات طالب مدير
DELETE /api/students/:id حذف طالب مدير
GET /api/students/:id/dashboard لوحة تحكم الطالب مستخدم
POST /api/students/import استيراد طلاب مدير

 المعلمون

الطريقة المسار الوصف الصلاحيات
GET /api/teachers جلب جميع المعلمين مستخدم
POST /api/teachers إضافة معلم جديد مدير
GET /api/teachers/:id جلب بيانات معلم مستخدم
PUT /api/teachers/:id تحديث بيانات معلم مدير
DELETE /api/teachers/:id حذف معلم مدير
GET /api/teachers/:id/dashboard لوحة تحكم المعلم معلم

 المقررات

الطريقة المسار الوصف الصلاحيات
GET /api/courses جلب جميع المقررات مستخدم
POST /api/courses إضافة مقرر جديد مدير
GET /api/courses/:id جلب بيانات مقرر مستخدم
PUT /api/courses/:id تحديث بيانات مقرر مدير
DELETE /api/courses/:id حذف مقرر مدير
GET /api/courses/available المقررات المتاحة مستخدم
GET /api/courses/:id/details تفاصيل المقرر مستخدم

 الحضور

الطريقة المسار الوصف الصلاحيات
GET /api/attendance سجل الحضور مستخدم
POST /api/attendance تسجيل حضور معلم
POST /api/attendance/bulk تسجيل جماعي معلم
GET /api/attendance/range الحضور حسب الفترة مستخدم
PUT /api/attendance/:id تحديث الحضور معلم
DELETE /api/attendance/:id حذف سجل حضور معلم

 الدرجات

الطريقة المسار الوصف الصلاحيات
GET /api/grades جميع الدرجات مستخدم
POST /api/grades إضافة درجة معلم
POST /api/grades/bulk إضافة جماعية معلم
GET /api/grades/student/:id/summary ملخص درجات الطالب مستخدم
PUT /api/grades/:id تحديث درجة معلم
DELETE /api/grades/:id حذف درجة معلم

 متابعة أولياء الأمور

الطريقة المسار الوصف الصلاحيات
GET /api/followups جميع المتابعات مستخدم
POST /api/followups إضافة متابعة معلم
GET /api/followups/overdue المتابعات المتأخرة مستخدم
PATCH /api/followups/:id/status تحديث الحالة معلم
GET /api/followups/student/:id متابعات طالب مستخدم
DELETE /api/followups/:id حذف متابعة معلم

 التقارير

الطريقة المسار الوصف الصلاحيات
GET /api/reports/overview نظرة عامة مستخدم
GET /api/reports/students تقرير الطلاب مستخدم
GET /api/reports/attendance تقرير الحضور مستخدم
GET /api/reports/grades تقرير الدرجات مستخدم
GET /api/reports/comprehensive تقرير شامل مدير

واجهة المستخدم

الألوان الرئيسية

```css
:root {
  --primary-color: #2c5aa0;      /* الأزرق الأساسي */
  --primary-dark: #1e3d72;       /* الأزرق الداكن */
  --primary-light: #4a7cd8;      /* الأزرق الفاتح */
  --secondary-color: #27ae60;    /* الأخضر */
  --accent-color: #e74c3c;       /* الأحمر */
  --warning-color: #f39c12;      /* البرتقالي */
  --text-dark: #2c3e50;          /* النص الداكن */
  --text-light: #7f8c8d;         /* النص الفاتح */
  --bg-light: #f8f9fa;           /* الخلفية الفاتحة */
  --bg-white: #ffffff;           /* الخلفية البيضاء */
}
```

المكونات الرئيسية

لوحة التحكم

· إحصائيات سريعة
· رسوم بيانية تفاعلية
· الأنشطة الحديثة
· أداء الطلاب

إدارة الطلاب

· جدول بيانات تفاعلي
· نموذج إضافة/تعديل
· البحث والتصفية
· استيراد/تصدير البيانات

التقارير

· تقارير تفصيلية
· رسوم بيانية متعددة
· تصدير بتنسيقات مختلفة
· فلاتر متقدمة

 الأمان

المصادقة

· نظام JWT للمصادقة
· تشفير كلمات المرور
· صلاحيات المستخدمين
· جلسات آمنة

الصلاحيات

· المدير: صلاحيات كاملة
· المعلم: إدارة الفصول والطلاب
· ولي الأمر: متابعة الأبناء فقط

حماية البيانات

· تحقق من صحة البيانات
· معالجة الأخطاء
· منع هجمات SQL Injection
· حدود معدل الطلبات

🐳 Docker Deployment

ملف docker-compose.yml

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongo:27017/quran-school
    depends_on:
      - mongo

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  mongo:
    image: mongo:5.0
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
```

متغيرات البيئة

```env
NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb://mongo:27017/quran-school
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRE=30d
CLIENT_URL=http://localhost:3000
```

 الاختبار

اختبارات الوحدة

```bash
# Backend tests
cd backend
npm test

# Frontend tests
cd frontend
npm test
```

اختبارات API

```bash
# Using curl examples
curl -X GET http://localhost:5000/api/students \
  -H "Authorization: Bearer YOUR_TOKEN"

curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@quran-school.com","password":"admin123"}'
```

 أداء النظام

قاعدة البيانات

· فهارس محسنة للاستعلامات السريعة
· تجميع البيانات للإحصائيات
· استعلامات فعالة

الواجهة الأمامية

· تحميل متقطع للبيانات
· التخزين المؤقت
· تحسين الصور والأصول

الخادم

· ضغط البيانات
· حدود معدل الطلبات
· معالجة الأخطاء

 المساهمة في المشروع

إعداد بيئة التطوير

1. Fork المشروع
2. Clone النسخة الخاصة بك
3. إنشاء فرع للميزة الجديدة
4. إجراء التغييرات
5. اختبار التغييرات
6. فتح Pull Request

دليل المساهمة

· اتبع نمط الترميز الموجود
· اكتب تعليقات توضيحية للكود
· أضف اختبارات للميزات الجديدة
· تحديث الوثائق

الإبلاغ عن الأخطاء

1. ابحث في Issues الحالية
2. أنشئ Issue جديد
3. ارفق خطوات إعادة إنتاج المشكلة
4. أضف لقطات الشاشة إذا أمكن
.

📞 الدعم والاتصال


· البريد الإلكتروني: anskajamal@gmail.com
.الموقع الشخصي : https://mohamedalamin.infinityfree.me


· دليل المستخدم
· دليل المطور
· API Documentation

🚀 خطط التطوير المستقبلية

الميزات القادمة

· تطبيق جوال
· نظام الدفع الإلكتروني
· الفصول الافتراضية
· التقييم الذاتي
· مكتبة الوسائط
· نظام الإشعارات

التحسينات المخطط لها

· تحسين الأداء
· إضافة المزيد من الاختبارات
· تحسين تجربة المستخدم
· دعم لغات إضافية



التقنيات المستخدمة

· React
· Node.js
· MongoDB
· Express.js
· Chart.js
· Docker

---


```

## 📋 ملفات الوثائق الإضافية

### docs/api.md
```markdown
# 📡 وثائق API - نظام إدارة تعليم القرآن الكريم

## المقدمة
هذه الوثائق تغطي جميع نقاط نهاية API المتاحة في النظام.

## الأساسيات
- **Base URL:** `http://localhost:5000/api`
- **التشفير:** JSON
- **المصادقة:** Bearer Token

## الرؤوس المطلوبة
```http
Authorization: Bearer YOUR_JWT_TOKEN
Content-Type: application/json
```

نمط الاستجابة

جميع الاستجابات تتبع النمط التالي:

```json
{
  "success": true,
  "message": "رسالة توضيحية",
  "data": { ... },
  "pagination": { ... }
}
```

أكواد الحالة

· 200 - نجاح
· 201 - تم الإنشاء
· 400 - طلب غير صالح
· 401 - غير مصرح
· 403 - ممنوع
· 404 - غير موجود
· 500 - خطأ في الخادم

... [المزيد من التفاصيل عن كل endpoint]

```

### docs/setup.md
```markdown
# ⚙️ دليل الإعداد والتركيب

## المتطلبات الأساسية
- Node.js 18+
- MongoDB 5.0+
- npm 8+

## الإعداد خطوة بخطوة

### 1. تنزيل المشروع
```bash
git clone https://github.com/your-username/quran-learning-platform.git
cd quran-learning-platform
```

2. إعداد الباكند

```bash
cd backend
npm install
cp .env.example .env
```

3. تكوين البيئة

عدل ملف .env:

```env
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/quran-school
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRE=30d
```

... 

```

### docs/user-guide.md
```markdown
# 📖 دليل المستخدم - نظام إدارة تعليم القرآن الكريم

## المقدمة
مرحباً بك في نظام إدارة تعليم القرآن الكريم. هذا الدليل سيساعدك على استخدام النظام بفعالية.

## بدء الاستخدام

### تسجيل الدخول
1. افتح المتصفح واذهب إلى `http://localhost:3000`
2. أدخل بيانات الدخول:
   - المدير: admin@quran-school.com / admin123
   - المعلم: teacher1@quran-school.com / teacher123

### لوحة التحكم
- شاهد الإحصائيات العامة
- تابع الأنشطة الحديثة
- استعرض الرسوم البيانية

... [mohamed]
```
